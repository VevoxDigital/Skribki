'use strict';

const expect  = require('expect.js'),
      path    = require('path');

const srcPath = 'lib/logger';

describe(srcPath, () => {

  var svr, logger;

  beforeEach(() => {
    
  });

});
